version: '3.9'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medicitas_frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - medicitas_shared_net

networks:
  medicitas_shared_net:
    external: true
